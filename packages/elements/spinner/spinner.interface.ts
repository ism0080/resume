export interface SpinnerProps extends CommonProps {
  /**
   * Used as the primary spinner color. The stroke occupying
   * the majority of the spinner
   */
  color?: string
  /**
   * Specified the secondary background stroke color
   */
  tint?: string
}
